Get-Content updater\updater.py# D&D v26 Phase 2B

Fixes: Goals dialog parent bug, BeatCard Qt signal bool bug, duplicate beat edit dialog, robust MP3 playback errors, direct Artist edit, favorites UI, Goal edit on double-click, backup validation/import placeholder, diagnostics.

The backup importer deliberately validates without overwriting live data until a conflict-safe merge layer is added.

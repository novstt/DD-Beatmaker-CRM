D&D v26 TEST V5

Fix: ArtistsTab no longer references a non-existent audio player. Beat playback remains owned by BeatsTab.

Run backend from project root:
    docker compose up --build

Then desktop:
    cd desktop
    python main.py
